#include<iostream>
#include<fstream>
using namespace std;

class Compiler {
  public: 
     void compile(void);
}
void Compiler::compile (void) {
  
  
}

int main() {
  Compiler compiler
  compiler.compile(void)
  
 return 0; 
}
