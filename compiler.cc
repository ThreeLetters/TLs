#include<iostream>
#include<fstream>
#include <string>
using namespace std;

class Compiler {
  public: 
     void compile(string);
}
void Compiler::compile (string) {
  
  
}

int main() {
  
  string str;
  
  Compiler compiler
  compiler.compile(str)
  
 return 0; 
}
